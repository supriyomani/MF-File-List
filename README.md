# MF-File-List
The code is to pull a list of file from mainframe directory and write it to a csv file. The idea is to poll the directory on a regular basis and get the list of files updated since last poll.
